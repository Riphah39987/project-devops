apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  environment: "production"
  log_level: "INFO"
  app_name: "devops-sample-app"
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: default
type: Opaque
stringData:
  db_password: "changeme"
  api_key: "changeme"
  secret_key: "changeme"
